<h1>Articles</h1>

Our blog have <%= pluralize(@articles.count, 'article') %>. 

<ul>
  <% @articles.each do |article| %>
    <% if !article.archived? %>
    <li><%= link_to( article.title, article ) %></li>
    <% end %>
  <% end %>
</ul>

<%= link_to 'New Article', new_article_path %>
